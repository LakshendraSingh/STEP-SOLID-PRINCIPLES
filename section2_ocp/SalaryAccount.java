package section2_ocp;

/**
 * Section 2 - Subsection 4: SalaryAccount extending Account
 */
public class SalaryAccount extends Account {
    public SalaryAccount(int accountNumber, String accountHolder, double initialBalance) {
        super(accountNumber, accountHolder, initialBalance);
    }
}
